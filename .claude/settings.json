{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$TOOL_INPUT\" | grep -q 'git commit'; then cd /Users/hydai/workspace/vibe/localtype && cargo fmt --all -- --check && cargo clippy --workspace -- -D warnings && cargo test --workspace && (cd ui && npm run build); fi"
          }
        ]
      }
    ]
  }
}
